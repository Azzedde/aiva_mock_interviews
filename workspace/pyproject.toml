[project]
name = "aiva_mock_interviews"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "openai",
    "python-dotenv",
    "requests",
    "pydub",
    "PyPDF2",
    "pyaudio",
    "SpeechRecognition",
]
